# 2D MRI experiment configuration
description:
  A 2D MRI experiment using a CPMG based sequence.
programs:
  - 2D_SLICE_MRI_CPMG
parameters:
  interpolation:
    dtype: int
    unit: ''
    min: 1
    group: postprocessing
defaults:
  plots:
    - Raw
    - Echo_Integrals
  order: 10
