# Diffusion PGSE experiment configuration
description:
  A Diffusion PGSE experiment.
programs:
  - DIFF_PGSE
parameters:
  sample_shift:
    dtype: float
    unit: us
    group: acquisition
  gaussian_lb:
    dtype: float
    unit: ''
    group: acquisition
defaults:
  plots:
    - Raw
    - FT
  order: 6
