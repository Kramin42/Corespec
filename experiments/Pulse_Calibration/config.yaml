# Pulse Calibration experiment configuration
description:
  An experiment for calibrating pulse power/width.
programs:
  - FID
parameters:
  start_width:
    dtype: float
    unit: us
    group: calibration
  end_width:
    dtype: float
    unit: us
    group: calibration
  steps:
    dtype: int
    group: calibration
  int_width:
    dtype: float
    min: 0
    unit: kHz
    group: calibration
defaults:
  plots:
    - FID
    - Calibration
