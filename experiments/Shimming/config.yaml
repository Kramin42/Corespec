# Shimming helper experiment configuration
hidden: True
description:
  A Shimming helper experiment.
programs:
  - FID
parameters:
  gaussian_lb:
    dtype: float
    unit: ''
    group: acquisition
defaults:
  plots:
    - Raw
    - FT
  order: 5