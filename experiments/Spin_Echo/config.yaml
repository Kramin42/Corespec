# Spin Echo experiment configuration
description:
  A Spin Echo experiment.
programs:
  - SpinEcho
parameters:
  sample_shift:
    dtype: float
    unit: us
    group: acquisition
defaults:
  plots:
    - Raw
    - FT
  order: 5
