# CPMG experiment configuration
description:
  A CPMG experiment.
programs:
  - CPMG
  - TTLControl
parameters:
  flow_delay:
    dtype: int
    unit: 's'
    group: flow_control
defaults:
  plots:
    - Echo_Integrals
    - Echo_Envelope
