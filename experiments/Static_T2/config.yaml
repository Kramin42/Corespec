# CPMG experiment configuration
description:
  A CPMG experiment.
programs:
  - CPMG
parameters:
  flow_delay:
    dtype: int
    unit: 's'
    group: flow_control
  oil_rel_H_density:
    dtype: float
    unit: ''
    group: calibration
defaults:
  plots:
    - Echo_Integrals
    - Echo_Envelope
